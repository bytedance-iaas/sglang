Date: 2026-04-22
TaskStatus: completed

Task scope:
  Port NVIDIA GR00T-N1.7 (Qwen3-VL backbone + DiT/flow-matching action head) to
  SGLang.  Target: load /data/models/GR00T-N1.7-3B end-to-end and answer
  chat-completion requests with a predicted [40, 132] action trajectory that
  matches Isaac-GR00T's Gr00tPolicy.get_action within numerical tolerance.

Current summary:
  Feature branch `groot-n17-port`.  F1-F9 landed previously.  F11
  (MaskedFlashAttention / DiT attention via sglang flash-varlen) landed
  2026-04-22; all 10 pytest cases green in ~37s on CUDA bf16.  Only F10
  (documentation) remains.

  F11 deliverable (new module + port migration):
    - python/sglang/srt/layers/attention/masked_flash_attn.py:
      MaskedFlashAttention (diffusers-named to_q/to_k/to_v/to_out
      projections) with two dispatch paths — fixed-length self-attn and
      per-key bool-mask cross-attn (varlen K/V gather + flash_attn_varlen_func).
      No SDPA fallback; no ForwardContext dependency.  CUDA bf16/fp16 only.
      Placed under srt/layers/attention/ (namespace package, no __init__.py
      side-effects) rather than multimodal_gen/runtime/layers/attention/
      because the latter's __init__.py eagerly imports DiffGenerator and
      LocalAttention, pulling in trimesh + ForwardContext that are not
      available outside a full diffusion-pipeline boot.
    - python/sglang/srt/models/groot_n1d7.py:
      BasicTransformerBlock.attn1 is now MaskedFlashAttention; diffusers
      Attention + _sdpa_context are fully removed.  forward_batch is
      threaded through DiT / AlternateVLDiT / SelfAttentionTransformer /
      Gr00tN1d7ActionHead._process_vl / Gr00tN1d7ActionHead.get_action /
      Gr00tN1d7.forward, mirroring alpamayo_r1.py.  load_weights is
      unchanged (submodule names match diffusers exactly).
    - test/manual/models/test_groot_n17.py:
      test_dit_parity + test_action_head_get_action_shape_and_determinism +
      test_gr00t_forward_emits_pred_traj_via_history_traj +
      test_full_parity_against_reference now run on CUDA bf16 (native
      checkpoint dtype).  Tolerances: per-layer 5e-3 (flash-varlen vs SDPA
      bf16 reduction-order drift); integrated F9 1e-1 (4 Euler steps × 32
      DiT + 4 VL-SA layers × per-layer drift compounds to ~6e-2 max-abs,
      well under task-level robot-joint tolerance).  New
      test_masked_flash_attention_varlen covers the varlen K/V gather
      path end-to-end.

  F9 deliverable (prior):
    End-to-end parity vs Isaac-GR00T reference Gr00tN1d7ActionHead on real
    /data/models/GR00T-N1.7-3B action_head.* weights.  On CUDA bf16:
    max-abs diff = ~5.9e-2 (well under the updated 1e-1 tolerance).

Features (see feature_list.md for acceptance criteria):
  F1  Gr00tN1d7Config + HF registration                    completed
  F2  Action-head primitive modules                        completed
  F3  Action-head top-level module + Euler loop            completed
  F4  Top-level Gr00tN1d7 + load_weights                   completed
  F5  VLM hidden-state extraction at select_layer=16       completed
  F6  Multimodal processor                                 completed
  F7  API plumbing (history_traj / pred_traj)              completed
  F8  start.sh + test_online_full.py                       completed
  F9  Accuracy parity vs Gr00tPolicy                       completed
  F10 Documentation                                        not_completed
  F11 DiT attention via MaskedFlashAttention               completed

Main gaps:
  Only F10 (documentation) remains.  Docs should mirror alpamayo_r1.md:
  overview, launch command, sample request (history_traj in / pred_traj
  out), embodiment tag table, response schema.

Next steps:
  F10: create docs/supported_models/vla_models/groot_n17.md and register
  it in docs/supported_models/vla_models/index.rst.
