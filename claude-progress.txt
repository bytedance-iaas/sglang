Date: 2026-04-23
TaskStatus: completed

Task scope:
  Port NVIDIA GR00T-N1.7 (Qwen3-VL backbone + DiT/flow-matching action head) to
  SGLang.  Target: load /data/models/GR00T-N1.7-3B end-to-end and answer
  chat-completion requests with a predicted [40, 132] action trajectory that
  matches Isaac-GR00T's Gr00tPolicy.get_action within numerical tolerance.

Current summary:
  Feature branch `groot-n17-port`.  F1-F13 landed.  F14 landed 2026-04-23:
  test_online_full.py is now the single canonical end-to-end tutorial
  script.  It uses Isaac-GR00T's LeRobotEpisodeLoader for dataset
  loading, emits a clear install-guidance warning when the `gr00t`
  package isn't importable (pointing users at
  https://github.com/NVIDIA/Isaac-GR00T.git), and supersedes the
  duplicate scripts/groot_parity/open_loop_eval_sglang.py which has
  been removed.  Only F10 (documentation) remains.

  Note on numbering: the user labeled this task "AS F13" but F13 was
  already taken by the 2026-04-23 feature-marker scrub commit.  Used
  F14 to avoid collision.

  F14 deliverable (script merge):
    - test_online_full.py (rewrite):
      * Loads trajectory via LeRobotEpisodeLoader instead of the old
        hand-rolled parquet + decord video wrangling.
      * Uses Isaac-GR00T's Gr00tPolicy(strict=False) solely for its
        modality_configs + state_action_processor (normalize/un-
        normalize statistics); inference itself goes to sglang.
      * Wraps Isaac imports in try/except ImportError that writes a
        clear stderr warning with `git clone
        https://github.com/NVIDIA/Isaac-GR00T.git` + `pip install -e .`
        instructions and a `uv run` fallback, then exits 1.  Users on
        a default sglang install see the actionable warning instead
        of a raw ModuleNotFoundError traceback.
      * CLI args: --traj-id, --dataset, --embodiment-tag,
        --action-horizon, --steps, --sglang-url.
      * Prints per-step latency + aggregate physical-space MSE/MAE
        vs episode ground truth; cites the Isaac Gr00tPolicy
        reference (MSE=0.003289, MAE=0.037619 on DROID traj 1) for
        calibration.
    - scripts/groot_parity/open_loop_eval_sglang.py: removed (merged
      into test_online_full.py).  The now-empty
      scripts/groot_parity/ directory is also gone.
    - feature_list.md / claude-progress.txt: this F14 entry +
      summary refresh.

  Prior landmarks (unchanged):
    F13 (2026-04-23): scrubbed all `F1`..`F13` feature-tracker
    markers from groot runtime / test / launcher / parity-script
    files.  Ledger ownership stays in feature_list.md +
    claude-progress.txt only.
    F12 (2026-04-23): dropped Isaac-GR00T source path dep from
    test_groot_n17.py so the pytest suite runs on a default sglang
    install.
    F11: MaskedFlashAttention with forward_batch threading through
    the DiT / AlternateVLDiT / SelfAttentionTransformer stack.
    F9 parity (Isaac action head @ bf16, max-abs ~5.9e-2 ≤ 1e-1)
    was validated pre-F12; equivalent signal comes from
    test_online_full.py's DROID physical-space MSE (now powered by
    LeRobotEpisodeLoader).

Features (see feature_list.md for acceptance criteria):
  F1  Gr00tN1d7Config + HF registration                    completed
  F2  Action-head primitive modules                        completed
  F3  Action-head top-level module + Euler loop            completed
  F4  Top-level Gr00tN1d7 + load_weights                   completed
  F5  VLM hidden-state extraction at select_layer=16       completed
  F6  Multimodal processor                                 completed
  F7  API plumbing (history_traj / pred_traj)              completed
  F8  start.sh + test_online_full.py                       completed
  F9  Accuracy parity vs Gr00tPolicy                       completed
  F10 Documentation                                        not_completed
  F11 DiT attention via MaskedFlashAttention               completed
  F12 Drop Isaac-GR00T path dep from manual test           completed
  F13 Scrub feature-list markers from groot code           completed
  F14 Single e2e tutorial script via LeRobotEpisodeLoader  completed

Main gaps:
  Only F10 (documentation) remains.  Docs should mirror alpamayo_r1.md:
  overview, launch command, sample request (history_traj in / pred_traj
  out), embodiment tag table, response schema.

Next steps:
  F10: create docs/supported_models/vla_models/groot_n17.md and register
  it in docs/supported_models/vla_models/index.rst.
