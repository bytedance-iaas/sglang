Date: 2026-04-23
TaskStatus: completed

Task scope:
  Port NVIDIA GR00T-N1.7 (Qwen3-VL backbone + DiT/flow-matching action head) to
  SGLang.  Target: load /data/models/GR00T-N1.7-3B end-to-end and answer
  chat-completion requests with a predicted [40, 132] action trajectory that
  matches Isaac-GR00T's Gr00tPolicy.get_action within numerical tolerance.

Current summary:
  Feature branch `groot-n17-port`.  F1-F9 + F11 landed previously.  F12
  landed 2026-04-23: `test/manual/models/test_groot_n17.py` no longer
  depends on the `/data/dongmao_dev/Isaac-GR00T` source checkout, so the
  pytest suite runs cleanly on a default sglang install with just
  `/data/models/GR00T-N1.7-3B` weights present.  Only F10 (documentation)
  remains.

  F12 deliverable:
    - test/manual/models/test_groot_n17.py:
      Removed 3 Isaac-source-dependent tests (`test_embodiment_mlp_parity`,
      `test_dit_parity`, `test_full_parity_against_reference`) and the
      helpers that loaded Isaac files into sys.modules
      (`_load_isaac_file`, `_load_isaac_action_head_class`,
      `_load_action_head_weights_bf16`, `_install_torchao_diffusers_stub`)
      along with the `ISAAC_GR00T` path constant.  File shrank from ~898
      lines to ~491 lines.  The remaining 7 tests cover every port
      surface (config, MaskedFlashAttention varlen, action-head shape +
      determinism, weight-routing split, processor stateless contract,
      F7 VLA plumbing, F5+F7 model-forward → pred_traj).  Open-loop MSE
      vs DROID GT is the canonical port-verification signal and lives in
      test_online_full.py.
    - feature_list.md:
      Added F12 block (goal, acceptance, passes: completed).  F2 and F9
      descriptions left untouched per CLAUDE.md §8; their completion
      state is preserved (tests did pass when authored) and F12's
      description explains that their verification migrated to
      test_online_full.py.
    - claude-progress.txt:
      This update.

  Prior landmarks (unchanged):
    F9 parity (Isaac action head @ bf16, max-abs ~5.9e-2 ≤ 1e-1) was
    validated pre-F12; see 2026-04-22 run of the now-removed
    test_full_parity_against_reference.  Equivalent signal going forward
    comes from test_online_full.py's DROID physical-space MSE.
    F11 delivered MaskedFlashAttention (srt/layers/attention/
    masked_flash_attn.py) threading forward_batch through DiT /
    AlternateVLDiT / SelfAttentionTransformer.

Features (see feature_list.md for acceptance criteria):
  F1  Gr00tN1d7Config + HF registration                    completed
  F2  Action-head primitive modules                        completed
  F3  Action-head top-level module + Euler loop            completed
  F4  Top-level Gr00tN1d7 + load_weights                   completed
  F5  VLM hidden-state extraction at select_layer=16       completed
  F6  Multimodal processor                                 completed
  F7  API plumbing (history_traj / pred_traj)              completed
  F8  start.sh + test_online_full.py                       completed
  F9  Accuracy parity vs Gr00tPolicy                       completed
  F10 Documentation                                        not_completed
  F11 DiT attention via MaskedFlashAttention               completed
  F12 Drop Isaac-GR00T path dep from manual test           completed

Main gaps:
  Only F10 (documentation) remains.  Docs should mirror alpamayo_r1.md:
  overview, launch command, sample request (history_traj in / pred_traj
  out), embodiment tag table, response schema.

Next steps:
  F10: create docs/supported_models/vla_models/groot_n17.md and register
  it in docs/supported_models/vla_models/index.rst.
