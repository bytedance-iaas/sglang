name: Lint

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install pre-commit hook
        run: |
          python -m pip install pre-commit
          pre-commit install

      - name: Run pre-commit checks
        run: SKIP=no-commit-to-branch pre-commit run --all-files --show-diff-on-failure

  cpu-ut:
    needs: lint
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          df -h

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install -e "python/[dev]"

      - name: Run registered CPU default suite
        working-directory: test
        run: |
          python3 run_suite.py --hw cpu --suite default

      - name: Run registered CPU stage-a suite
        working-directory: test
        run: |
          python3 run_suite.py --hw cpu --suite stage-a-cpu-only


  arc-smoke-1gpu:
    needs: lint
    runs-on: [self-hosted, 1-gpu-runner]
    timeout-minutes: 15
    steps:
      - name: Show runner labels
        run: echo "Running on self-hosted 1-gpu runner"

      - name: Check GPU visibility
        run: nvidia-smi --query-gpu=name --format=csv,noheader

      - name: Check docker sidecar
        run: docker version
